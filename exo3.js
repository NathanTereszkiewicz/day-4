let numbers = [10, 20, 30, 40];
let totalelement = numbers.length;
let sum = 0;
for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
}
let random = sum / totalelement;
let countAboveAverage = 0
